schema_version = "1.0"
module_boundaries = []

[behavior]
intended_behavior = "Exercise the protected organization ruleset with one harmless README-only migration canary."
proof = "README.md contains the disposable S11 migration marker; no package or runtime file changes."

[characterization]
captured_behavior = "Supportability Audit package behavior is unchanged because the canary edits only delivery documentation and review evidence."
proof = "The authenticated base/head characterization and fixed repository validation remain the executable proof."

[separation_of_concerns]
before = "The migration canary was prepared under the predecessor organization rule."
after = "The same harmless canary head proves the replacement deterministic rule without changing package ownership."
boundaries = []

[architecture]
dependency_direction = "No production dependency changes; README and review evidence remain outside the runtime package."
reviewed_paths = [
    ".supportability-review.toml",
    "README.md",
]

[responsibility_boundary]
path = "README.md"
owns = "Disposable documentation marker for protected ruleset verification."
does_not_own = "Supportability Audit runtime, validator behavior, Gate implementation, or production ruleset configuration."

[incremental_refactor]
target = "Verify one exact organization-ruleset replacement on an unmerged harmless pull request."
completed_step = "Prepared the canary and bound truthful no-production-change review evidence."

[review_handoff]
summary = "DERIVED_FROM_AUTHENTICATED_EVIDENCE"
remaining_risks = ["DERIVED_FROM_AUTHENTICATED_EVIDENCE"]

[human_review]
naming = "The S11 migration marker and evidence name their temporary delivery role."
cohesion = "README owns the marker; the review packet owns claims about this diff."
intended_behavior = "All eight deterministic lanes qualify the current head; connector availability is irrelevant."
reviewability = "Two metadata files change; no package, source, dependency, command, threshold, exclusion, waiver, or bypass changes."
