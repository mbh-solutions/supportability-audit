schema_version = "1.0"

[behavior]
intended_behavior = "Keep Supportability Audit product code and behavior unchanged while qualifying the S01 one-time focused-review lifecycle on a protected Python target."
proof = "The exact pull-request diff contains only .supportability-review.toml; GitHub-hosted base/head characterization and target-native Validate evidence must bind the protected run before merge."

[characterization]
captured_behavior = "The existing exhaustive-pm-audit-contract-v3 scenario covers both production modules on the exact base and head without adding or changing target code."
proof = ".supportability-characterization.json binds the existing scenario; the required workflow publishes authenticated exact-run base and head artifacts."

[separation_of_concerns]
before = "Product modules, tests, and repository validation retain their existing responsibilities."
after = "Those boundaries remain unchanged; this file records only protected S01 lifecycle qualification evidence."

[architecture]
dependency_direction = "No product dependency, import, module, or ownership boundary changes."
reviewed_paths = [
    ".supportability-review.toml",
    "src/supportability_audit/__init__.py",
    "src/supportability_audit/validate_repository.py",
]

[responsibility_boundary]
path = "src/supportability_audit/validate_repository.py"
owns = "Deterministic validation of the Supportability Audit repository package."
does_not_own = "Connector orchestration, Supportability Gate implementation, or this disposable protected qualification lifecycle."

[incremental_refactor]
target = "No target refactor; qualify the frozen S01 connector lifecycle without changing product code."
completed_step = "Prepared one evidence-only review handoff on a disposable protected branch."

[review_handoff]
summary = "Review the evidence-only Python target qualification for exact serial focuses 1 through 8 and post-review completion reuse."
remaining_risks = ["Exact review-run, request, completion, final-run, and merge identities remain pending until GitHub creates them."]

[human_review]
naming = "Disposable proof refs and lifecycle evidence use explicit S01 qualification names."
cohesion = "The change is confined to the existing structured review-evidence file."
intended_behavior = "Target product code and runtime behavior remain unchanged."
reviewability = "One structured evidence file exposes the complete target diff."

[[module_boundaries]]
path = "src/supportability_audit/__init__.py"
owner_path = "src/supportability_audit/validate_repository.py"
basis = "responsibility"
justification = "The package marker remains inside the unchanged repository-validation boundary."

[[module_boundaries]]
path = "src/supportability_audit/validate_repository.py"
owner_path = "src/supportability_audit/validate_repository.py"
basis = "responsibility"
justification = "The validator module continues to own deterministic repository-package validation."
